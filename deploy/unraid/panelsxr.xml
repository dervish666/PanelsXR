<?xml version="1.0"?>
<!-- First-draft Unraid Community Applications template for Panel.
     Fill the TODO fields (they need the repo pushed to GitHub first), confirm
     the host port, then feed this to https://ca.unraid.net/submit — its live
     scan validates the XML + ca_profile.xml and previews the listing. -->
<Container version="2">
  <Name>PanelsXR</Name>
  <Repository>dervish/panelsxr:latest</Repository>
  <Registry>https://hub.docker.com/r/dervish/panelsxr/</Registry>
  <Network>bridge</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>TODO-github-issues-url</Support>
  <Project>TODO-github-repo-url</Project>
  <Overview>Panel is a WebXR comic reader that streams your self-hosted Komga library into a Meta Quest 3 headset. Resume where your iPad left off, turn pages on the thumbstick, and browse a 3D library of your series. Runs in the Quest browser (no store, no sideload).

Set KOMGA_URL to your Komga address and KOMGA_API_KEY to a Komga API key (Komga, Account Settings, API Keys). The key is injected server-side and never reaches the browser.

IMPORTANT: WebXR needs HTTPS. Access this behind a reverse proxy (SWAG / Nginx Proxy Manager / Cloudflare Tunnel). A plain http address loads the 2D page but the Quest browser will not enter VR.</Overview>
  <Category>MediaApp:Books</Category>
  <WebUI>http://[IP]:[PORT:80]/</WebUI>
  <Icon>TODO-icon-url</Icon>
  <ExtraParams/>
  <PostArgs/>
  <Config Name="WebUI Port" Target="80" Default="8677" Mode="tcp" Description="Port for the Panel web UI" Type="Port" Display="always" Required="true" Mask="false">8677</Config>
  <Config Name="Komga URL" Target="KOMGA_URL" Default="" Description="Your Komga base URL, e.g. http://192.168.1.10:8080" Type="Variable" Display="always" Required="true" Mask="false"></Config>
  <Config Name="Komga API Key" Target="KOMGA_API_KEY" Default="" Description="A Komga API key (Komga, Account Settings, API Keys). Injected server-side; never sent to the browser." Type="Variable" Display="always" Required="true" Mask="true"></Config>
  <Config Name="Basic Auth User" Target="PANEL_USER" Default="panel" Description="Username for the built-in HTTP basic-auth login (only used if a password is set below)." Type="Variable" Display="always" Required="false" Mask="false">panel</Config>
  <Config Name="Basic Auth Password" Target="PANEL_PASSWORD" Default="" Description="Set a password to lock the WHOLE app (and the Komga proxy) behind HTTP basic auth. The Quest browser remembers it, so it's a one-time login. STRONGLY recommended before exposing Panel to the internet. Leave blank only for LAN-only access or if you gate it another way (e.g. Cloudflare Access)." Type="Variable" Display="always" Required="false" Mask="true"></Config>
</Container>
